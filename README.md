# Bratislava Travel Guide (Static)

This project generates a static travel guide website using Python, YAML, and Jinja2.

## Requirements
- Python 3
- `PyYAML`
- `Jinja2`

## Generate the site
```bash
python3 generate.py
```

Output files are written to `dist/`. Deploy by copying `dist/` to GitHub Pages.
